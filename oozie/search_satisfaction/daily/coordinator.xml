<?xml version="1.0" encoding="UTF-8"?>
<coordinator-app xmlns="uri:oozie:coordinator:0.4"
    name="search_satisfaction_daily-coord"
    frequency="${coord:days(1)}"
    start="${start_time}"
    end="${stop_time}"
    timezone="Universal">

    <controls>
        <!--
        By having materialized jobs not timeout, we ease backfilling incidents
        after recoverable hiccups on the dataset producers.
        @TODO not sure if this is needed
        -->
        <timeout>-1</timeout>

        <!--
          search satisfaction daily aggregation is pretty light. Allow
          concurrency for backfill.
        -->
        <concurrency>6</concurrency>

        <!--
          Use a reasonable throttle to feed the backfill concurrency.
        -->
        <throttle>6</throttle>
    </controls>

    <datasets>
        <!--
          Include discovery datasets files. $search_satisfaction_datasets_file will be used as the
          input and output events.
        -->
        <include>${search_satisfaction_datasets_file}</include>
    </datasets>

    <input-events>
        <data-in name="mediawiki_cirrussearch_request_input" dataset="mediawiki_cirrussearch_request">
            <start-instance>${coord:current(0)}</start-instance>
            <end-instance>${coord:current(23)}</end-instance>
        </data-in>

        <data-in name="event_testsearchsatisfaction2_input" dataset="event_testsearchsatisfaction2">
            <start-instance>${coord:current(0)}</start-instance>
            <end-instance>${coord:current(23)}</end-instance>
        </data-in>
    </input-events>

    <output-events>
        <data-out name="search_satisfaction_daily_output" dataset="search_satisfaction_daily">
            <instance>${coord:current(0)}</instance>
        </data-out>
    </output-events>

    <action>
        <workflow>
            <app-path>${workflow_file}</app-path>
            <configuration>

                <property>
                    <name>year</name>
                    <value>${coord:formatTime(coord:nominalTime(), "y")}</value>
                </property>
                <property>
                    <name>month</name>
                    <value>${coord:formatTime(coord:nominalTime(), "M")}</value>
                </property>
                <property>
                    <name>day</name>
                    <value>${coord:formatTime(coord:nominalTime(), "d")}</value>
                </property>
                <property>
                    <!-- To mark directory done after success -->
                    <name>search_satisfaction_daily_dataset_directory</name>
                    <value>${coord:dataOut('search_satisfaction_daily_output')}</value>
                </property>

            </configuration>
        </workflow>
    </action>
</coordinator-app>
