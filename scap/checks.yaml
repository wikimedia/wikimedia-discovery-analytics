checks:
    # Virtualenvs must be built from a debian stretch machine, aka stat1007. These will
    # run on both stretch and buster. If the venv is built from buster it will not run
    # on stretch.
    virtualenv_analytics:
        type: script
        stage: fetch
        timeout: 300
        group: analytics
        command: build_deployment_virtualenvs.sh
